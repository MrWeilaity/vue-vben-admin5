spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/vben_admin
    username: postgres
    password: ${PGSQL_PASSWORD:123456}
  data:
    redis:
      host: redis
      port: 6379
      password: ${REDIS_PASSWORD:123456}
      database: 0
logging:
  level:
    com.vben: info
    org.springframework: warn
